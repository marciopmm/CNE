<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="CNE.EmployeeMainPage">
	<ContentPage.Content>
		<ScrollView Padding="10, 40, 10, 10">
			<StackLayout Orientation="Vertical"
						 Spacing="10"
						 HorizontalOptions="CenterAndExpand"
						 VerticalOptions="FillAndExpand">
				<Image Source="LogoCNE200.jpg" Aspect="AspectFit" />
				<Label x:Name="lblNome"
					   Style="{DynamicResource CaptionStyle}"
					   Text="Olá, Fulano"
					   HorizontalTextAlignment="Center"
					   HorizontalOptions="CenterAndExpand"
					   VerticalOptions="CenterAndExpand" />
				<Label Text="Bem vindo à sua página de estatísticas CNE."
					   HorizontalOptions="CenterAndExpand"
					   VerticalOptions="CenterAndExpand" />
				<Label x:Name="lblVisualizado"
					   Style="{DynamicResource TitleStyle}"
					   Text="Seu perfil foi visualizado XXX vezes." 
					   HorizontalOptions="CenterAndExpand"
					   VerticalOptions="CenterAndExpand"/>
				<Button x:Name="btnPerfil"
						Text="Mantenha seu perfil atualizado"
						HorizontalOptions="CenterAndExpand"
					   VerticalOptions="CenterAndExpand" />
			</StackLayout>
		</ScrollView>
	</ContentPage.Content>
</ContentPage>
